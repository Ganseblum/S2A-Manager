# 2026-06-28 工作日志

## S2A-Manager 项目工作

### 安全审计
- 对项目代码做了全面安全审计，确认无后门、无数据上报、无 API Key 泄露
- Admin API Key 使用 AES-256-GCM 加密存储，前端展示脱敏（••••••）
- 唯一外部链接 z30.top 是作者推广链接，不会自动发请求

### 清理推广链接
- 删除了 `src/components/app/project-promo-links.tsx` 中的 z30.top 推广链接
- 删除了 README.md 中的参考中转站链接
- 保留了 GitHub 项目链接

### 部署文档
- 整理了 `同机部署手册.md`（针对 macOS 本地 / 同机部署场景）
- 整理了 `部署手册.md`（通用完整版）
- 新增 `scripts/preflight-check.sh` 部署前环境检测脚本，检测 Node.js/npm/PostgreSQL/端口/数据库/依赖/构建状态，已有的自动提示跳过

### 用户环境信息
- macOS 本地开发环境
- Node.js v22.22.2、npm 10.9.7、PostgreSQL 18.4 已安装
- Sub2API 项目在 `/Users/hang/Documents/myProject/sub2api`
- GitHub fork 到 Ganseblum/S2A-Manager

### 云服务器实际部署
- 云服务器 IP: 154.40.43.181，域名 ai.youc.online（Sub2API，后端 8080）
- 使用 pnpm 替代 npm，配了 npmmirror 国内镜像源
- 遇到 package.json 中 `start` 脚本硬编码 `-p 3000`，覆盖了 .env 的 PORT=3001，已修复为 `"next start"`
- pnpm 需要 `pnpm approve-builds` 放行 prisma/esbuild/unrs-resolver 的构建脚本
- systemd 使用 `/usr/bin/pnpm start` 和 `Environment=PORT=3001`
- Nginx 新增 s2a.youc.online 反代到 127.0.0.1:3001（增量 conf，不影响原有 ai.youc.online）
- certbot --nginx 自动配 HTTPS + 自动续期
- 首次访问无需账号密码，自动跳初始化页面自行创建管理员

### 文档更新
- 完全重写 `云服务器部署手册.md`（v2.0），基于实际部署经验，含 pnpm/Nginx 反代/HTTPS/升级/回滚全流程
- 修复本地 `package.json` 的 start 脚本（移除硬编码 -p 3000）
- 同步更新 `同机部署手册.md` 为 pnpm 命令
